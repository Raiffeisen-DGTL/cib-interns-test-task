server.port=${PORT:8080}

spring.datasource.driver-class-name =com.mysql.cj.jdbc.Driver
spring.datasource.url=${CLEARDB_DATABASE_URL:jdbc:mysql://localhost:3306/storage?useUnicode=true&serverTimezone=UTC&useSSL=true&verifyServerCertificate=false}
spring.datasource.username=${CLEARDB_DATABASE_USERNAME:root}
spring.datasource.password=${CLEARDB_DATABASE_PASSWORD:sashakar1}
spring.jpa.generate-ddl=false


spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.baseline-on-migrate=true

