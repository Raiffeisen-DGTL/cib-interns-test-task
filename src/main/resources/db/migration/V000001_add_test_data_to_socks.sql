CREATE TABLE socks (
                         id int NOT NULL AUTO_INCREMENT,
                         color varchar(50) NOT NULL,
                         cottonPart int NOT NULL ,
                         quantity int NOT NULL,
                         PRIMARY KEY (id)
);